NODE_ENV=development
PORT=8080
HOST=0.0.0.0

# JWT
JWT_SECRET=your-super-secret-key-change-in-production

# Header obrigatório em produção (todas as requisições devem enviar X-API-Key com este valor)
# Deixe vazio em dev para não exigir o header
API_SECRET=

# Database (Prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/yasapi
